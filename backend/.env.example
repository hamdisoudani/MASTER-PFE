# Port (Railway sets this automatically)
PORT=3001

# URL of the Python agent AG-UI endpoint
# In Railway: set to your agent service public URL

# Comma-separated CORS origins, or * for all
CORS_ORIGINS=*