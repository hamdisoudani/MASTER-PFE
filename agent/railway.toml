[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "langgraph dev --host 0.0.0.0 --port ${PORT:-2024} --no-browser --allow-blocking"
healthcheckPath = "/ok"
healthcheckTimeout = 120
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
