[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "sh -c 'langgraph dev --host 0.0.0.0 --port $PORT --no-browser --allow-blocking'"
healthcheckPath = "/ok"
healthcheckTimeout = 120
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
